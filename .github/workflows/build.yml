# SPDX-License-Identifier: MIT

name: Build RK3229 ImmortalWrt Firmware RK

on:
  workflow_dispatch:
  push:
    branches:
      - rk322x

jobs:
  build_openwrt:
    name: Build ImmortalWrt for RK3229 (ARMv7)
    runs-on: ubuntu-latest
    env:
      CONFIG_URL: "https://raw.githubusercontent.com/pitekkate/immortalwrt/rk322x/Rk3229-box.config" 
      TARGET_PROFILE: "rk3229-evb"

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Basic Environment
        run: |
          sudo rm -rf /usr/share/dotnet /etc/mysql /etc/php || true
          sudo apt-get purge -y azure-cli* docker* ghc* zulu* llvm* firefox google* dotnet* powershell* openjdk* mysql* php* mongodb* snap* || true
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential libncurses5-dev flex bison libssl-dev python3 python3-pip rsync unzip zip \
            git wget curl bc kmod patch make gcc g++ zlib1g-dev libzstd-dev \
            libncurses5-dev libreadline-dev libssl-dev libelf-dev liblzma-dev \
            libudev-dev libusb-1.0-0-dev libcurl4-openssl-dev

      - name: Clone Source Code
        run: |
          git clone --depth=1 --branch rk322x https://github.com/pitekkate/immortalwrt  openwrt

      - name: Update & Install Feeds
        working-directory: ./openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Download and Apply Config
        working-directory: ./openwrt
        run: |
          echo "📥 Mengunduh Rk3229-box.config..."
          curl -sL "$CONFIG_URL" -o .config

          echo "🔧 Membersihkan konfigurasi lama..."
          sed -i '/CONFIG_TARGET_/d' .config
          sed -i '/CONFIG_TARGET_BOARD/d' .config
          sed -i '/CONFIG_TARGET_SUBTARGET/d' .config
          sed -i '/CONFIG_TARGET_PROFILE/d' .config
          sed -i '/CONFIG_TARGET_ARCH_PACKAGES/d' .config

          echo "🔧 Menetapkan target Rockchip ARMv7..."
          echo "CONFIG_TARGET_rockchip=y" >> .config
          echo "CONFIG_TARGET_rockchip_armv7=y" >> .config
          echo "CONFIG_TARGET_rockchip_armv7_DEVICE_${TARGET_PROFILE}=y" >> .config
          echo "CONFIG_KERNEL_PATCHVER=\"6.6\"" >> .config
          echo "CONFIG_LINUX_6_6=y" >> .config

          make defconfig

      - name: 🔍 Validasi Target Build
        id: validate_config
        continue-on-error: false
        working-directory: ./openwrt
        run: |
          echo "🔍 Memvalidasi apakah .config sudah benar untuk RK3229 ARMv7..."

          if grep -q 'CONFIG_TARGET_rockchip=y' .config && \
             grep -q 'CONFIG_TARGET_rockchip_armv7=y' .config && \
             grep -q "CONFIG_TARGET_rockchip_armv7_DEVICE_${TARGET_PROFILE}=y" .config; then
            echo "✅ Target config valid: rockchip/armv7/${TARGET_PROFILE}"
          else
            echo "❌ Target config salah!"
            echo "Harus memiliki:"
            echo "  CONFIG_TARGET_rockchip=y"
            echo "  CONFIG_TARGET_rockchip_armv7=y"
            echo "  CONFIG_TARGET_rockchip_armv7_DEVICE_rk3229-evb=y"
            echo ""
            echo "Isi .config saat ini:"
            cat .config | grep CONFIG_TARGET_
            exit 1
          fi

      - name: Stop Workflow Jika Salah Target
        if: failure() || steps.validate_config.outputs.status == 'failed'
        run: |
          echo "🛑 Build dibatalkan karena .config salah target."
          exit 1

      - name: Download Packages
        working-directory: ./openwrt
        run: |
          echo "📦 Mengunduh semua paket..."
          make download -j$(nproc)

      - name: Build Firmware with Log Output
        id: build_firmware
        continue-on-error: true
        working-directory: ./openwrt
        run: |
          echo "🛠️ Mulai build firmware untuk RK3229 (ARMv7)"
          make -j$(nproc) V=s 2>&1 | tee ../build.log

          echo "🔍 Mencari file firmware hasil build..."
          find ./bin/targets/rockchip/armv7/ -type f -name "*.*" || true

      - name: Check Build Status
        id: check_build
        shell: bash
        run: |
          if grep -q 'make[1-9][0-9]*: ***' ../build.log; then
            echo "::set-output name=status::failed"
            exit 1
          else
            echo "::set-output name=status::success"
          fi

      - name: Prepare Artifact
        if: success()
        working-directory: ./openwrt
        run: |
          mkdir -p ../artifact
          IMG_FILE=$(find ./bin/targets/rockchip/armv7/ -type f -name '*-sysupgrade.tar.gz' -o -name '*.img' | head -1)

          if [ -f "$IMG_FILE" ]; then
            echo "✅ Firmware ditemukan: $IMG_FILE"
            cp $IMG_FILE ../artifact/
          else
            echo "❌ Firmware tidak ditemukan!"
            find ./bin/targets/ -type f
            exit 1
          fi

      - name: Upload Firmware
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: RK3229_ImmortalWrt_Firmware
          path: ./artifact/*

      - name: Upload Error Log (if failed)
        if: failure() || steps.check_build.outputs.status == 'failed'
        uses: actions/upload-artifact@v4
        with:
          name: build-error-log
          path: ../build.log

      - name: Upload Full Build Log (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: full-build-log
          path: ../build.log
